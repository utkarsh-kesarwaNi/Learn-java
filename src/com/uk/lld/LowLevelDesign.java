package com.uk.lld;

public class LowLevelDesign {

    /*
    * What is Low level design ?
    *
    * Low level design (LLD) is the phase of the software engineering lifecycle where
    * broad architectural components (defined in High-Level Design) are decomposed into granular, executable, and testable blueprints.
    * It defines the exact classes, interfaces, data structures, algorithms, and concurrent interactions required to implement the system.
    *
    * LLD bridges the gap between system architecture and actual code. A rigorous LLD must optimize for two primary metrics:
    * 1. High cohesion: Elements within a single class or module strictly belong together and serve a single, well-defined purpose.
    * 2. Low coupling: Classes have minimal dependencies on other concrete classes, interacting instead through well-defined abstractions (interfaces).
    * */
}
